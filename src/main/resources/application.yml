spring:
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:work
    username: work
    password: Start123!
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true
